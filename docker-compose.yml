version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - PORT=$PORT
      - DATATBASE_URL=$DATABASE_URL
    ports:
      - "3003:3003"
    volumes:
      - .:/usr/src/app
